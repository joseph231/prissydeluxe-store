// /js/admin/orders.js
document.addEventListener('DOMContentLoaded', () => {
  const ordersTable = document.getElementById('ordersTable');
  if (ordersTable) ordersTable.innerHTML = '<div class="card">No orders yet (UI stub)</div>';
});
