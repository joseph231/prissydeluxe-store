// /js/admin/inventory.js
document.addEventListener('DOMContentLoaded', () => {
  const invList = document.getElementById('inventoryList');
  if (invList) invList.innerHTML = '<div class="card">No inventory (UI stub)</div>';
});
